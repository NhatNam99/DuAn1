<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.edu.poly.duan1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".UpdatePatientActivity" />
        <activity android:name=".AddPatientActivity" />
        <activity
            android:name=".PatientDetailsActivity"
            android:label="Patient Details"
            android:theme="@style/AppTheme." />
        <activity android:name=".ChangePasswordActivity" />
        <activity
            android:name=".PatientManagementActivity"
            android:label="@string/title_activity_patient_management"
            android:theme="@style/AppTheme" />
        <activity android:name=".RegisterInformationActivity">

        </activity>
    </application>

</manifest>